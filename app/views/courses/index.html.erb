<div class="container-fluid mt-5">
  <div class="row">
    <!-- Sidebar Filters -->
    <div class="col-md-3", style="height: 50rem;
    overflow-y: scroll;">
      <%= render 'sidebar_filters' %>
    </div>
    
    <!-- Main Content Area -->
    <div class="col-md-9">
      <%= render 'search_bar' %>
      
      <%= turbo_frame_tag "courses_list" do %>
        <%= render 'courses_list', universities: @available_universities %>
      <% end %>
    </div>
  </div>
</div>

<%# Add a hidden form to preserve search query when filters are applied %>
<% if params[:query].present? %>
  <%= form_tag courses_path, method: :get, id: "preserve_query_form", style: "display: none;" do %>
    <%= hidden_field_tag :query, params[:query] %>
    <% params.except(:controller, :action, :query).each do |key, value| %>
      <%= hidden_field_tag key, value %>
    <% end %>
  <% end %>
  
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Add event listeners to all filter forms to preserve the search query
      document.querySelectorAll('form[action*="courses"]').forEach(function(form) {
        if (form.id !== 'preserve_query_form') {
          form.addEventListener('submit', function(e) {
            // Don't prevent default submission
            // Just ensure the query parameter is included
            if (!form.querySelector('input[name="query"]')) {
              const queryInput = document.createElement('input');
              queryInput.type = 'hidden';
              queryInput.name = 'query';
              queryInput.value = '<%= params[:query] %>';
              form.appendChild(queryInput);
            }
          });
        }
      });
    });
  </script>
<% end %>

<style>
  [data-search-target="queryResults"] {
    position: absolute;
    background: white;
    width: 87.6%;
    z-index: 1050;
    border: 1px solid #ccc;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
  }
  .col-md-4 {
    position: relative;
  }
  .clear-search {
    display: none;
    outline: none !important;
  }
  .form-control:not(:placeholder-shown) ~ .clear-search {
    display: block;
  }
  .clear-search {
    cursor: pointer;
    opacity: 0.6;
    transition: opacity 0.2s;
  }
  .clear-search:hover {
    opacity: 1;
  }
  .card-title {
    font-size: 0.95rem;
  }

  .carousel-control-prev-icon,
  .carousel-control-next-icon {
    background-size: 100% 100%;
    opacity: 0.8;
  }

  .carousel-control-prev-icon:hover,
  .carousel-control-next-icon:hover {
    opacity: 1;
  }

  .card:hover {
    transform: translateY(-3px);
    transition: transform 0.2s ease-in-out;
    box-shadow: 0 6px 20px rgba(0,0,0,0.08);
  }
  #similarCoursesCarousel .carousel-control-prev,
  #similarCoursesCarousel .carousel-control-next {
    width: auto;
    top: 35%;
    transform: translateY(-50%);
    z-index: 2;
  }

  #similarCoursesCarousel .carousel-control-prev {
    left: -60px; /* Close to left edge inside the margin */
  }

  #similarCoursesCarousel .carousel-control-next {
    right: -60px; /* Close to right edge inside the margin */
  }

  #similarCoursesCarousel {
    margin-left: 100px;
    margin-right: 100px;
  }
  /* Previous Button - margin-left */
  .carousel-control-prev.custom-control {
    margin-left: 25px;
    left: -60px; /* Keeps it inside the carousel but near the edge */
  }

  /* Next Button - margin-right */
  .carousel-control-next.custom-control {
    margin-right: 25px;
    right: -60px; /* Keeps it inside the carousel but near the edge */
  }

  /* General button styles */
  #similarCoursesCarousel .carousel-control-prev,
  #similarCoursesCarousel .carousel-control-next {
    width: auto;
    top: 35%;
    transform: translateY(-50%);
    z-index: 2;
  }
  .mb-45 {
    margin-bottom: 45px;
  }
  .ml-45 {
    margin-left: 45px;
  }
</style>

